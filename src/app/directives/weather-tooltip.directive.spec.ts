import { WeatherTooltipDirective } from './weather-tooltip.directive';

describe('WeatherTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
