import { TempColorDirective } from './temp-color.directive';

describe('TempColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TempColorDirective();
    expect(directive).toBeTruthy();
  });
});
